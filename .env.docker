# ==============================================================================
# Docker Environment Configuration
# ==============================================================================
# This file is used automatically when running with docker-compose
#
# ⚠️  CRITICAL SECURITY WARNING ⚠️
# Generate a secure BETTER_AUTH_SECRET before deploying to production!
# Run: openssl rand -base64 32
# ==============================================================================

# Node Environment
NODE_ENV=production

# GPU Configuration
HASHCAT_DEVICE_TYPE=cpu

# Database (configured by docker-compose)
DATABASE_URL=postgresql://autopwn:autopwn_password@postgres:5432/autopwn

# ⚠️  Better Auth - MUST CHANGE THIS IN PRODUCTION!
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=REPLACE_THIS_WITH_SECURE_SECRET_GENERATED_BY_OPENSSL_RAND_BASE64_32
BETTER_AUTH_URL=http://localhost:3001

# Backend Port
PORT=3001

# File Storage Paths (Docker volumes)
PCAPS_PATH=/data/pcaps
DICTIONARIES_PATH=/data/dictionaries
JOBS_PATH=/data/jobs

# Frontend API URL
NEXT_PUBLIC_API_URL=http://localhost:3001