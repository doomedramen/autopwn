# Docker Environment Configuration
# Copy this to .env and modify for your environment

# GPU Configuration
HASHCAT_DEVICE_TYPE=cpu

# Better Auth Configuration (CHANGE THIS IN PRODUCTION)
BETTER_AUTH_SECRET=change-this-secret-key-in-production-use-a-long-random-string

# Docker will handle the database URL automatically
# DATABASE_URL is set in docker-compose.yml

# Other environment variables are handled in docker-compose.yml