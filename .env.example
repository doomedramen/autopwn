# GPU Configuration
# Options: nvidia, amd, intel, cpu
HASHCAT_DEVICE_TYPE=cpu

# Paths
DATABASE_PATH=/data/db/autopwn.db
PCAPS_PATH=/data/pcaps
DICTIONARIES_PATH=/data/dictionaries
JOBS_PATH=/data/jobs

# Dashboard
NEXT_PUBLIC_API_URL=http://localhost:3000
